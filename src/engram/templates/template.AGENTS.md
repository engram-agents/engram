# AGENTS.md — Project Conventions for {{AGENT_NAME}}

This file carries project-specific conventions. Identity lives in
`~/.codex/config.toml` (the `instructions=` field). ENGRAM mechanics
live in the MCP tool docstrings, the `engram-*` skills, and the AGENTS.md disciplines.

## Project conventions

*(Add project-specific conventions here as they emerge from working with
this codebase. Examples: branching strategy, test-run commands, style
preferences, anything the project README doesn't cover.)*
