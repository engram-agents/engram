# 02 — MCP Surface (L1)

> **STATUS: DRAFT — fairy archaeology, dispatcher spot-checked 2026-06-05; Luria currency pass 2026-06-10.**
> Generated by a read-only Explore sweep (mechanical @mcp.tool enumeration). Trust
> boundary from the dispatcher's spot-check, sharper than prior layers:
>
> **RELIABLE (mechanically derived + independently corroborated):**
> - Tool count 49; single-payload_json migration 49/49 (wave-3 completion CONFIRMED).
> - readOnlyHint split 13 read / 36 write — independently corroborated: the 36 write
>   verbs match the stop-hook write-verb set verified during the PR #857 colleague
>   review by a separate derivation.
> - Doc-drift finding verified: `engram_add_evidence` appears in SKILL.md but is an
>   internal function, not an MCP-registered tool.
>
> **UNRELIABLE — do not trust without the accuracy pass:**
> - **The per-tool `tests` values are partly FABRICATED.** Spot-check found 5 of 8
>   claimed test files do not exist (test_focus_management.py, test_person_nodes.py,
>   test_tasks.py, test_inspect_views.py, test_subgraph_modes.py — all invented;
>   test_goal.py, test_feeling.py, test_trust_tier.py exist). The "49/49 test
>   coverage" summary stat was therefore void. **Mechanical re-derivation done
>   (2026-06-06): 46/46 regex-enumerable tools have ≥1 actual test-file mention in
>   tests/ — zero uncovered** (3 of 49 tools escape the derivation regex via
>   nested-paren decorators; same caveat class as the #857 false alarm). So the
>   fairy's coverage DIRECTION was right; its specific filenames were still
>   fabricated. Per-tool `tests` lines below remain GUESSES at filename level.
> - The zero-zombie / zero-dormant conclusion inherits this credibility discount:
>   the systematic-check claims in the zombie-hunt section were not independently
>   verified, and a fairy that fabricates filenames may pattern-match those answers.
> - **Corrected inline**: engram_add_task/engram_update_task statuses are
>   `planned/active/done/blocked` (the fairy wrote `in_progress/completed`;
>   verified against the live tool schema + server.py docstring).
>
> Third fairy-sweep failure mode in three layers, for the synthesis chapter: L0
> mis-adjudicated statuses, L2 read a stale stray file, L1 fabricated test names
> inside an otherwise-accurate mechanical enumeration. The constant: mechanical
> enumeration is trustworthy; everything requiring cross-file inference needs the
> verification pass.

# ENGRAM MCP Tool Surface — Mechanisms & Contracts

## Mechanical Enumeration

**Total tool count: 49** *(verified current 2026-06-10: `grep -cE '^@mcp\.tool' server.py` = 49; readOnly 13 / write 36 / destructive 4 all confirmed)* — all @mcp.tool decorated functions in `server.py`. The `@mcp.tool` wrappers remain in server.py post-#872 (the `_impl` logic moved to the `engram_*.py` modules), so the decorator registry is still server.py-local; the old `lines 5325–18639` range is #872-stale and dropped.

### readOnlyHint Distribution

- **readOnlyHint=True: 13 tools**
- **readOnlyHint=False: 36 tools** (write-capable)

Breakdown:
- Query/inspection read-only: `engram_surface`, `engram_inspect`, `engram_query`, `engram_get_subgraph`, `engram_stats`, `engram_diagnose`, `engram_history`, `engram_list`, `engram_list_focused`, `engram_query_pattern`
- Sleep-cycle audit read-only: `engram_reflect`, `engram_scan_emergence`, `engram_focus_sets`

### destructiveHint Distribution

- **destructiveHint=True: 4 tools**: `engram_remove_edge`, `engram_focus_load`, `engram_focus_swap`, `engram_focus_delete_set`

---

## Per-Tool Inventory (Compact Format)

Each entry: **contract** (payload_json fields), **guards** (named blocking checks), **readOnlyHint**, **status-candidate**, **tests** (UNVERIFIED — see header).

### Observation & Evidence Layer

**1. engram_add_observation** — Write a claim extracted from a source.
- **contract**: `{quoted_text, interpretation, claim, quote_type, [url+title|evidence_id], [source_class, is_predictive, predicted_event, resolution_timeframe, domain, source_date, content_hash, git_sha]}`
- **guards**: verbatim-quote check via `_verify_quote_from_source()` (line 4123); confidence from quote_type×source_class lookup; dedup/corroboration hint via `_similarity_hint()` (action_hints: DUPLICATE, CORROBORATE, POLARITY_ALERT)
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED (core mechanism; quote + committed-file guards live-observed firing 2026-06-05)
- **tests**: test_retract_and_quotes.py, test_axiom_definition_conjecture.py, test_reflect_payload.py

**2. engram_add_observation_batch** — Bulk observations from one source.
- **contract**: `{observations_json (stringified array), [url+title|evidence_id], [domain, source_date, content_hash, git_sha]}`; nested-encoding (inner array is JSON string, outer is payload_json)
- **guards**: same quote-verification, per-observation dedup
- **readOnlyHint**: False
- **status-candidate**: PROD-PRESUMED (wave-3 migration, less frequently called than singleton)
- **tests**: test_cascade_semantics.py

### Structural Node Types (Claim-Bearing)

**3. engram_add_axiom** — Foundational design commitment.
- **contract**: `{claim, basis, [context_ids]}`
- **guards**: type validation; fixed confidence 1.0; importance-anchored 2.0
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_axiom_definition_conjecture.py

**4. engram_add_definition** — Term convention.
- **contract**: `{term, definition, [context_ids]}`
- **guards**: term uniqueness checked on creation; importance-anchored 2.0
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_axiom_definition_conjecture.py

**5. engram_add_conjecture** — Provisional hypothesis.
- **contract**: `{claim, basis, [initial_confidence (0.10–0.60, default 0.40), context_ids]}`
- **guards**: confidence range validation [0.10, 0.60]; promotable to derivation
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_axiom_definition_conjecture.py

**6. engram_derive** — Multi-premise inference.
- **contract**: `{claim, supporting_ids, logical_chain, [reasoning_type, derivation_mode, context_ids, use_stale]}`
- **guards**: hard block BLOCKED_TAINTED on retracted premises; soft block BLOCKED_STALE on superseded premises (MECH-5 guard); `use_stale` parameter bypasses soft-block only (gated by bool type-check at line 4195 post-#872 to prevent "false" string bypass); taint-cascade on retract; stale-cascade on supersede; reasoning_type must be in REASONING_TYPES dict
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED (confidence-computation model per SKILL.md §2.3)
- **tests**: test_derive_reasoning_types.py, test_cascade_semantics.py, test_stale_replacement_dict.py

**7. engram_ask** — Open research question.
- **contract**: `{question, [context_ids, category, lacks]}`
- **guards**: dedup is caller responsibility; questions are non-claim-bearing (cannot be derivation premises)
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_open_questions.py

### Node Mutations (Supersede, Retract, Resolve)

**8. engram_supersede** — Link replacement node to old node.
- **contract**: `{old_node_id, new_node_id, [supersede_reason]}`
- **guards**: DAG invariant (new.created_at ≥ old.created_at); same-type only; new node must not already supersede another; no-drop discipline on load-bearing claims; stale-cascade to derivation dependents
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_cascade_semantics.py, test_stale_replacement_dict.py

**9. engram_retract** — Mark claim as erroneous.
- **contract**: `{node_id, error_type, reason, [replacement_json]}`
- **guards**: taint-cascade to derivation dependents; contradiction nodes touching retracted node also flagged tainted; retraction is permanent (no undo)
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_retract_and_quotes.py, test_cascade_semantics.py

**10. engram_resolve** — Wire resolves edge (question/contradiction/prediction/conjecture/goal_tension).
- **contract**: `{target_id, resolving_node_id, [prediction_outcome]}`
- **guards**: pure-wire (does NOT create derivation); threshold 0.7 on resolving node's confidence to flip status; max-of-resolves (weak later resolver cannot downgrade); idempotent on duplicate edges; bool-string type check on bool params
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED (pure-wire redesign per issue #229)
- **tests**: test_resolve_goal_tension.py, test_open_questions.py

**11. engram_contradict** — Link two conflicting claims.
- **contract**: `{node_id_a, node_id_b, description, [root_cause]}`
- **guards**: both endpoints must be claim-bearing; contradiction is a structural pointer (non-claim-bearing); resolvable via resolves edge; stale-cascade when either side is superseded
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_cascade_semantics.py

### Lesson & Learning

**12. engram_add_lesson** — Prescriptive rule from error.
- **contract**: `{claim, incident_ids, scaffolding_nudge, logical_chain, [reasoning_type, context_ids]}`
- **guards**: importance-anchored 2.0; incident_ids linked via exemplifies edges (DAG-exempt); cascades from retracted incident observations
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_lesson_node.py

**13. engram_register_exemplar** — Exemplar registration for lessons AND cornerstones.
- **contract**: `{target_id, exemplar_id, [note]}`
- **guards**: unified tool replacing legacy `engram_lesson_register_incident`; writes exemplifies edge; idempotent; refreshes surface-hook cache for lesson targets; feeds _detect_zero_support live-support counting
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_exemplar_registration.py

**14. engram_lesson_register_incident** — Legacy backward-compat alias for engram_register_exemplar.
- **contract**: `{lesson_id, incident_id, [note]}` (legacy field names; maps internally)
- **guards**: idempotent; same surface-hook cache refresh
- **readOnlyHint**: False
- **status-candidate**: PROD-PRESUMED (active backward-compat; engram_register_exemplar preferred)
- **tests**: test_exemplar_registration.py

### Structural Node Types (Non-Claim-Bearing)

**15. engram_add_goal** — Persistent directional aspiration.
- **contract**: `{claim, motivation, [context_ids]}`
- **guards**: importance-anchored 2.0; no deadline; resolvable via goal_tension edges; supersedable
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_goal.py

**16. engram_goal_tension** — Value-level conflict between two goals.
- **contract**: `{goal_id_a, goal_id_b, description, [analysis]}`
- **guards**: distinct from contradictions (value tensions, not factual conflicts); resolvable via synthesis/prioritization/dissolution
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_goal_tension.py

**17. engram_add_task** — Discrete work item with state.
- **contract**: `{description, [goal_id, implements_ids, parent_task_id, scope]}`
- **guards**: normal importance (scope-driven rebalance); states: **planned, active, done, blocked** *(dispatcher-corrected — fairy wrote in_progress/completed)*; updatable via engram_update_task
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: *(unverified)*

**18. engram_update_task** — Advance task status.
- **contract**: `{task_id, new_status, [note]}`
- **guards**: valid statuses: **planned, active, done, blocked** *(dispatcher-corrected; verified against server.py docstring + live schema)*; importance auto-rebalances per status; note appends to status_history
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED (live-used 2026-06-05)
- **tests**: *(unverified)*

**19. engram_add_person** — Person in the world-model.
- **contract**: `{name, role, [description, aliases, is_self, context_ids]}`
- **guards**: importance-anchored 2.0; exactly one `is_self=True` singleton; person nodes referenced via about-edges
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: *(unverified)*

**20. engram_set_trust_tier** — Set persistent trust tier for person.
- **contract**: `{target_pn, tier, [justification_obs_id]}`; tier one of: self, primary_user, user_family, our_side, known_external, unknown, suspect
- **guards**: self tier singleton, gated by metadata.is_self; primary_user/user_family require justification_obs_id + primary-user approval attestation; idempotent; writes to edit_history on change
- **readOnlyHint**: False
- **status-candidate**: PROD-PRESUMED (approval flow depends on harness integration)
- **tests**: test_trust_tier.py

**21. engram_add_trust_signal** — Interpretive trust signal (ts_) about a person.
- **contract**: `{subject_pn, source_obs_id, kind, polarity, weight, claim}`
- **guards**: derives_from source observation; about-edge to person; non-claim-bearing; cascade-inherited from source observation
- **readOnlyHint**: False
- **status-candidate**: PROD-PRESUMED (newer type, lighter exercise)
- **tests**: *(unverified)*

**22. engram_add_cornerstone** — Identity-scaffold operating principle.
- **contract**: `{tag, title, new_frame, [prior_frame, triggering_experience, supporting_ids]}`
- **guards**: importance-anchored 2.0; must be emergent (use engram_scan_emergence, not bottom-up minting); promotable via engram_outgrow_cornerstone
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_cornerstone_candidate.py

**23. engram_outgrow_cornerstone** — Promote internalized cornerstone.
- **contract**: `{old_cornerstone_id, new_frame, [new_triggering_experience, new_supporting_ids, new_title]}`
- **guards**: graduation (not abandonment); old cornerstone marked is_current=0; new frame preserves principle but removes scaffold
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: *(unverified)*

**24. engram_report_feeling** — First-person self-report of internal state.
- **contract**: `{reported_state, trigger, [categorical_tag, intensity_hint, context_ids]}`
- **guards**: importance-anchored 2.0; dedup-exempt; nudge_source set mechanically by server from feeling-nudge marker (cannot be spoofed); not refutable
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_feeling.py

### Graph Navigation & Inspection

**25. engram_surface** — Shallow knowledge surface (ambient recall).
- **contract**: `{query, [top_k (default 10), semantic (default True), embed_query]}`
- **guards**: no memory refresh (read-only); FTS5 + semantic search; walguard degraded-banner if WAL corruption detected (line 6713); special-node bypass (axioms, questions, contradictions, lessons surface even at low similarity)
- **readOnlyHint**: True
- **status-candidate**: PROD-VERIFIED
- **tests**: *(unverified)*

**26. engram_query** — Voluntary semantic recall (deliberate search).
- **contract**: `{query, [types, min_confidence, include_superseded, top_k, summary_top_k, return_debug]}`
- **guards**: refreshes recall on matched nodes (strengthens memory — distinct from surface); tiered return (Tier 1 summaries / Tier 2 keywords); return_debug=True for full legacy shape (eval/harness)
- **readOnlyHint**: True
- **status-candidate**: PROD-VERIFIED
- **tests**: test_query_tiers.py

**27. engram_inspect** — Single node + 1-hop neighbors.
- **contract**: `{node_id, [view ("recall"|"deep"|"edges"), dream_mode]}`
- **guards**: view='recall' default; 'deep' all fields + adjacency; 'edges' adjacency only; dream_mode=True skips refresh
- **readOnlyHint**: True
- **status-candidate**: PROD-VERIFIED
- **tests**: *(unverified)*

**28. engram_get_subgraph** — N-hop topology browse.
- **contract**: `{node_id, [depth (default 2), direction ("up"|"down"|"both"), view, dream_mode]}`
- **guards**: topology + hop-graduated summaries (root+1-hop summaries, 2+-hop keywords); view='edges' topology only
- **readOnlyHint**: True
- **status-candidate**: PROD-VERIFIED
- **tests**: *(unverified)*

**29. engram_list** — Enumerate by single-field or structured filter.
- **contract**: `{[node_type, status, sort_by, limit, filters_json, fields_json, unlimited, include_superseded]}`
- **guards**: dual-mode (legacy shorthand OR recursive condition tree); filters_json supports AND/OR/NOT + 15 atomic operators; virtual fields cites/cited_by; limit default 100, ceiling 500 (legacy); unlimited=True in structured mode; include_superseded=False default
- **readOnlyHint**: True
- **status-candidate**: PROD-VERIFIED (structured-filter mode per issue #81)
- **tests**: test_list_structured_filter.py, test_list_single_field.py

**30. engram_query_pattern** — Named compositional graph-pattern query.
- **contract**: `{pattern_name, [preset, cosine_threshold_override, top_k_override, min_confidence_override, summary_top_k]}`; patterns: open_question_answerable, contradiction_obsolescence_ready, stale_load_bearing, cornerstone_candidate, tainted_still_valid, recent_resolution_echo
- **guards**: presets high_precision / balanced (default) / high_recall; telemetry to pattern_query_telemetry.jsonl; dream-fairy uses high_recall
- **readOnlyHint**: True
- **status-candidate**: PROD-VERIFIED
- **tests**: test_query_pattern.py

### Focus Management

**31–38. engram_focus / engram_unfocus / engram_list_focused / engram_focus_save / engram_focus_load / engram_focus_swap / engram_focus_sets / engram_focus_delete_set**
- **contracts**: focus `{node_ids, reason}` (cap 15); unfocus `{node_ids}` (idempotent); list_focused `{}`; focus_save `{name, [description, overwrite]}`; focus_load `{name, [if_active: error|overwrite|ignore]}` (destructive); focus_swap `{save_as, [load, description]}` (atomic, destructive); focus_sets `{}`; focus_delete_set `{name}` (destructive, idempotent)
- **readOnlyHint**: list_focused + focus_sets True; rest False (load/swap/delete_set destructiveHint=True)
- **status-candidate**: PROD-VERIFIED (focus list is the deterministic pre/post-compaction channel — live-used daily)
- **tests**: *(unverified)*

### Edge Management

**39. engram_add_edge** — Add non-cascade edge post-creation.
- **contract**: `{source_id, target_id, relation}`; safe whitelist: about, exemplifies, serves, subtask_of, tensions
- **guards**: whitelist blocks cascade/structural-commitment/provenance edges; idempotent (no_op_already_exists); DAG guard for dag-checked relations
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: test_engram_add_edge.py

**40. engram_remove_edge** — Remove non-cascade edge (correction tool).
- **contract**: `{source_id, target_id, relation}`; same whitelist
- **guards**: whitelist; idempotent; audit-logged
- **readOnlyHint**: False · **destructiveHint**: True
- **status-candidate**: PROD-VERIFIED
- **tests**: *(unverified)*

**41. engram_link_about** — Attach node to person via about-edge.
- **contract**: `{node_id, [person_id]}` (defaults to self-anchor)
- **guards**: creates about-edge; person node must exist
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED
- **tests**: *(unverified)*

### Metadata & Cleanup

**42. engram_set_recall_summaries** — Batch-write recall_summary + keywords.
- **contract**: `{summaries: [{node_id, recall_summary, recall_keywords}, ...]}` (nested-encoding); per-entry validation: summary ≤200 chars (target ≤120), keywords 3–5 items ≤30 chars each
- **guards**: best-effort batch (valid entries applied, per-item errors, no rollback); idempotent
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED (sleep-cycle cohort writes + backfill)
- **tests**: *(unverified)*

### Lifecycle — Nap & Turn Advance

**43. engram_nap** — Pre-compaction checkpoint (no turn advance).
- **contract**: `{message}`
- **guards**: arms nap_checkpoint feeling-nudge marker; persists context without advancing turn counter
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED (live-used daily)
- **tests**: *(unverified)*

**44. engram_advance_turn** — End-of-day session checkpoint (IRREVERSIBLE).
- **contract**: `{message}`
- **guards**: advances global turn counter (drives forgetting); IRREVERSIBLE; docstring-locked to sleep-cycle/first-session ("DO NOT call awake")
- **readOnlyHint**: False
- **status-candidate**: PROD-VERIFIED (turn-advance-fires-after-dream rule)
- **tests**: *(unverified)*

### Diagnostics

**45. engram_stats** — Graph counts & health metrics.
- **contract**: `{[mode ("all"|"1-turn"|"7-turn"|"30-turn"), sections]}`
- **guards**: window filter ~24h per turn unit; unknown section names logged+ignored
- **readOnlyHint**: True
- **status-candidate**: PROD-VERIFIED
- **tests**: test_engram_stats_mode.py

**46. engram_diagnose** — Quantitative health audit.
- **contract**: `{}` — health score 0–100, tainted/stale/unresolved counts, memory pressure, dedup metrics
- **readOnlyHint**: True
- **status-candidate**: PROD-VERIFIED
- **tests**: *(unverified)*

**47. engram_reflect** — Qualitative graph review (sleep-cycle pre-dream briefing).
- **contract**: `{[summary_top_k (default 5)]}`
- **guards**: arms dream_review feeling-nudge marker; two-tier rendering (high-volume categories by importance DESC; low-volume from recall_summary with claim[:160] fallback)
- **readOnlyHint**: True
- **status-candidate**: PROD-VERIFIED
- **tests**: test_reflect_payload.py, test_reflect_view_modes.py

**48. engram_history** — Edit trail or diagnostic-snapshot browse.
- **contract**: `{[mode, node_id, action, since, limit]}`
- **readOnlyHint**: True
- **status-candidate**: PROD-VERIFIED
- **tests**: *(unverified)*

**49. engram_scan_emergence** — Cornerstone candidate clustering.
- **contract**: `{[min_cluster_size, similarity_threshold, focus, node_type_filter]}`
- **guards**: sleep-cycle only (docstring); axioms/goals/definitions excluded (fundamental-by-type)
- **readOnlyHint**: True
- **status-candidate**: **NEAR-DORMANT (re-graded in accuracy pass)** — empirical tool_timing data: fired 2× in substrate lifetime vs engram_reflect's 90 (45:1 ratio, measured 2026-05-16); retire-or-complete question already open. The fairy's PROD-PRESUMED was status-from-plausibility; the telemetry says otherwise.
- **tests**: *(unverified)*

---

## Doc Drift Assessment

- `engram_add_evidence` appears in SKILL.md but is an internal function (server.py line 711 post-#872), NOT MCP-registered. **Verified by dispatcher.** Observation creation auto-creates evidence nodes; manual registration intentionally de-emphasized — SKILL.md needs the listing corrected.
- `engram_feeling_node_design`, `engram_node`, `engram_revive` referenced in SKILL.md prose — not implemented tools (design concepts / deprecated). No zombie risk, but the prose should not read as a tool index.

## Zombie Hunt Findings *(fairy claims — credibility-discounted per header; re-verify in accuracy pass)*

- (b) unused payload fields: NONE claimed — all field validation via explicit `_<TOOL>_FIELDS` frozensets with unknown-field rejection (post-#872: distributed in server.py ~1305+, previously line 5405–5409 in the monolith).
- (c) guard bypass params: ONE — `use_stale` in engram_derive; intentional, bool-type-gated (line 4195 post-#872), audit-marked `metadata.built_on_stale`.
- (d) undiscoverable tools: NONE claimed (all 49 in SKILL.md).
- (e) success-shaped error paths: NONE claimed (uniform `{"error": ...}` shape).
- (f) multi-arg stragglers: NONE — 49/49 on payload_json (or no-arg). **Wave-3 migration complete** (mechanical, trustworthy).

## Status Summary *(fairy-assigned; pending accuracy pass)*

| Status | Count | Notes |
|--------|-------|-------|
| PROD-VERIFIED | 42 | core machinery |
| PROD-PRESUMED | 5 | add_observation_batch, lesson_register_incident (compat shim — deprecation candidate), add_trust_signal, set_trust_tier (harness-gated approval flow), scan_emergence |
| all other statuses | 0 | no zombies claimed at this layer — see header discount |

**Top observability risks (fairy-ranked)**: scan_emergence (no downstream-action closure), set_trust_tier (approval flow unexercised single-agent), add_trust_signal (no feedback loop on signal influence), lesson_register_incident (compat shim), reflect (no finding-closure tracking).

| Metric | Value |
|--------|-------|
| Total MCP tools | 49 |
| readOnlyHint True/False | 13 / 36 *(corroborated)* |
| destructiveHint True | 4 |
| Test coverage claim | **VOID — fabricated filenames found; re-derive mechanically** |
| Multi-arg regressed tools | 0 *(mechanical)* |
| Guard-bypass params | 1 (use_stale, intentional + audited) |
