# ENGRAM Alpha runtime dependencies.
#
# Version-ranged where safe; hard-pinned where a specific version is
# load-bearing for correctness (see inline comments). For the exact
# known-working snapshot used by CI + the Docker test harness, see
# requirements-lock.txt.
#
# Install:  pip install -r requirements.txt
# Upgrade:  pip install -U -r requirements.txt

fastmcp>=3.0,<4
# Hard-pinned (not >=/<) — sentence-transformers 5.4.x hangs on HuggingFace
# Hub metadata fetch during cold-start, even with weights fully cached. The
# hang originates in huggingface_hub.file_download.get_hf_file_metadata, so
# huggingface_hub is equally load-bearing and pinned alongside. See #162.
sentence-transformers==5.3.0
huggingface_hub==1.8.0
# Hard-pinned to the version that created the existing vec_nodes indexes
# (Borges + Ariadne both on 0.1.9, verified). The vec0 KNN index over node
# embeddings replaces the O(N) pure-Python cosine fallback. This was declared
# in server.py but never in requirements.txt, so default installs (and the
# plugin migration venv-create) silently ran the slow fallback — and the #720
# backup crashed dropping vec_nodes under a venv lacking the module. See #728.
sqlite-vec==0.1.9
